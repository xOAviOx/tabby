node_modules/
dist/
.DS_Store
*.log

# Raw HF downloads and converted weights (M1). The real model is ~1 GB each way.
/models/
/public/models/*
*.safetensors

# ...except the few-KB synthetic model the fast loader tests run against.
!/public/models/tiny-test/

# Golden dumps (M2) -- 7.3 MB, regenerate with tools/golden.py
/public/golden/
/tests/golden/

# Python venv for tools/ (torch + transformers)
/.venv/

# Python bytecode from tools/
__pycache__/
*.pyc
