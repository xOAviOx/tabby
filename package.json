{
  "name": "browser-llm",
  "version": "0.0.0",
  "description": "Transformer LLM inference in the browser on WebGPU with hand-written WGSL kernels.",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/browser": "^4.1.11",
    "@webgpu/types": "^0.1.72",
    "playwright": "^1.62.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.11"
  },
  "private": true,
  "type": "module"
}
